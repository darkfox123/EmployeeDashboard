<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.digitalhomeland.employeedashboard">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:icon">
        <activity android:name=".DashboardActivity"></activity>
        <activity android:name=".ApplicationCreateActivity" />
        <activity android:name=".LandingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignupActivity" />
        <activity android:name=".AttendanceActivity" />
        <activity android:name=".AlertDailogActivity" />
        <activity
            android:name=".ApplicationNotifViewActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_application_notif_view"
            android:theme="@style/FullscreenTheme" />
        <activity android:name=".NotificationView" />
        <activity android:name=".NotificationActivity" />
        <activity android:name=".ApplicationViewActivity" />
        <activity android:name=".TasksActivity" />
        <activity android:name=".TaskViewActivity" />
        <activity android:name=".ViewRoster" />
        <activity android:name=".AttendanceView" />
        <activity android:name=".PreDashboard">

        </activity>
    </application>

</manifest>